set $mod Mod4

set $left h
set $down j
set $up k
set $right l
set $term alacritty
set $menu rofi -show drun

include input
include autostart
include output
include visual
include binds  # tier 0 - defaults, windows and workspaces control
include binds2 # tier 1 - tools, quick launch and custom binds

